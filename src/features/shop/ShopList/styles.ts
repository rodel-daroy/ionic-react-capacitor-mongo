import styled from 'styled-components';

export const StyledShopList = styled.ul`
    list-style: none;
    padding: 2.5px 2.5px 60px;
    display: grid;
    grid-gap: 15px;
    grid-template-columns: repeat(2, auto);
    grid-auto-flow: row;
`;
