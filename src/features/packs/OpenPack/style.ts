import styled from 'styled-components';

export const PackOpenVideoWrapper = styled.div`
    display: block;
    text-align: center;

    video {
        vertical-align: baseline;
        position: relative;
        width: 100%;
    }
`;
